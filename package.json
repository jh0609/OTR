{
  "name": "over-the-rainbow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "vite": "~6.0.0",
    "vitest": "~2.0.0"
  },
  "dependencies": {
    "phaser": "~3.80.0"
  }
}
